<isif condition="${!empty(pdict.config.boltEnable)}">
    <input type="hidden" class="check-bolt-account-exist" value="${URLUtils.https('Bolt-accountExists')}" />
    <input type="hidden" class="bolt-publishable-key" value="${pdict.config.boltMultiPublishableKey}" />
    <script id="bolt-embedded" type="text/javascript" src="${pdict.config.boltCdnUrl}/embed.js" defer> </script>
    <isscript>
        var assets = require('*/cartridge/scripts/assets.js');
        assets.addJs('/js/checkAccountExist.js');
        assets.addJs('/js/tokenization.js');
    </isscript>
</isif>